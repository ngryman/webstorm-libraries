/**
 * @param {string} description
 * @param {function} suite
 */
describe = function(description, suite) {};

/**
 * @param {string} description
 * @param {function} suite
 */
xdescribe = function(description, suite) {};

/**
 * @param {function} fn
 */
before = function(fn) {};

/**
 * @param {function} fn
 */
beforeEach = function(fn) {};

/**
 * @param {function} fn
 */
after = function(fn) {};

/**
 * @param {function} fn
 */
afterEach = function(fn) {};

/**
 * @param {string} description
 * @param {function} test
 */
it = function(description, test) {};

/**
 * @param {string} description
 * @param {function} test
 */
xit = function(description, test) {};